"use client"

export default function SignUp () {
    const handleCreateUserClicked = async () => {
        // Use react-form-hook and Zod to validate the data going into the function
        // Create a new user with the passed credentials
    }

    return (
        <div className='flex flex-col gap-y-4 p-5'>
            <h1>Sign Up</h1>
        </div>
    )
}